package com.traffic.interfaces{		/**	 * Describes the contract for disposable 	 * objects.	 */	public interface IDisposable 	{		/* ==================================================== */		/* = PUBLIC METHODS									  = */		/* ==================================================== */				/**		 * <p>A dispose helper method		 * for cleaning up objects		 * when they are no longer		 * needed.</p>		 */		function dispose():void;	}	}